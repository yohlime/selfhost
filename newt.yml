services:
  newt:
    image: fosrl/newt:1.12
    container_name: newt
    restart: unless-stopped
    env_file:
      - newt/.env
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - yohnet
